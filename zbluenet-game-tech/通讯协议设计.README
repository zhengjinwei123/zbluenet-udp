## udp 通讯报文设计

##### 一. 报文结构
```
1. 包体大小: 4个字节， 用于确认报文的完整性
2. 会话id: 4个字节, 用于识别客户端唯一性
3. 序号：4个字节，用于保证报文的顺序
4. 模块id: 4个字节, 可以把报文传递给对应模块
5. 发送时间戳: 8个字节，用于判断是否超时
6. 协议类型: 4个字节， 0(ack 确认报文)， 1(logic 业务报文) 
7. 协议id: 4个字节，用于区分业务
8. 协议内容(包体): 实际大小， 记录业务数据
```